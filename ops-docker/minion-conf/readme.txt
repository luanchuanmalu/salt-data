here for minion-conf is mainly for kubernetes node cluster servers.

when add new minions server to the cluster for kubernetes, generally we need change these config file here.
1. hosts
2. minionlist.ini

when we change docker-registry server's ip, or use new docker-registry server we need change the config file.
1. docker

when the kubernetes cluster service ip changed, we need change the nginx configure
1. nginx.conf
