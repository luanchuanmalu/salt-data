actually salt-master can't use these files to install, but I add it here to show you what you need to install on salt-master server.

when you manually do the install on salt-master, you can use the config file here named master, which already use the right configuration on port and file_root.
