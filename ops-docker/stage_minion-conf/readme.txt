here for stage_minion-conf is mainly for kubernetes node cluster servers for stage environment.
Share most of the conf in minion-conf, with some different:
1. different hosts
2. different minionlist.ini
3. different nginx.conf
please check the detail in the init.sls in stage_minion-conf
